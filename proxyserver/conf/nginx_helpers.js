function aport(r) {
    return process.env.PORTA;
}

function bport(r) {
    return process.env.PORTB;
}

function hostipa(r) {
    return process.env.HOSTIPA;
}

function hostipb(r) {
    return process.env.HOSTIPB;
}
